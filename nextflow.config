params {
    reads = "${projectDir}/test_data/*.fastq.gz"
    outdir = "${projectDir}/results"
}

profiles {

   test {
      process {
          executor = 'local'
          container = 'biocontainers/
fastqc:v0. 11.9-cv8'
      }
      docker{
          enabled = true
      }
   }
}
