params {
    reads = "${projectDir}/test_data/*.fastq"
    outdir = "${projectDir}/results"
}

profiles {
    test {
        process {
            executor = 'local'
            container = 'biocontainers/fastqc:v0.11.9_cv8'
        }

        docker.enabled = true
    }
}
